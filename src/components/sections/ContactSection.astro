---
import Section from "../layout/Section.astro";
import PixelButton from "../ui/PixelButton.astro";
import Icon from "../ui/Icon.astro";
import { socials, resumeUrl } from "../../data/socials";
import { profile } from "../../data/profile";

const email = socials.find((s) => s.id === "email");
const channelLinks = socials.filter((s) => s.id !== "email");

const protocolRows = [
  { label: "Status", value: profile.availability },
  { label: "Location", value: "Indonesia · GMT+7" },
  { label: "Focus", value: "Frontend, fullstack, reliability" },
  { label: "Mode", value: "Freelance / remote conversations" },
];
---

<Section id="contact" label="Contact" index="08" accent="mauve" class="section--dense">
  <div class="contact" data-reveal>
    <section class="contact__intro" aria-labelledby="contact-title">
      <span class="contact__state">
        <span aria-hidden="true"></span>
        handshake ready
      </span>

      <h2 id="contact-title" class="contact__title">Get in touch</h2>
      <p class="contact__copy">
        If you need a frontend developer who can also work across backend and
        reliability concerns, I'm open to talk.
      </p>

      <div class="contact__actions" aria-label="Primary contact actions">
        {email && (
          <PixelButton
            href={email.url}
            icon="mdi:send"
            variant="primary"
            class="contact__email-btn"
          >
            Send message
          </PixelButton>
        )}
        <PixelButton
          href={resumeUrl}
          iconRight="mdi:download"
          variant="ghost"
          target="_blank"
          rel="noreferrer"
        >
          Download CV
        </PixelButton>
      </div>

      <div class="contact__channels" aria-label="Connection channels">
        <p class="contact__channels-title">Connection channels</p>
        <ul role="list">
          {
            channelLinks.map((social) => (
              <li>
                <a
                  class="contact__channel"
                  href={social.url}
                  target="_blank"
                  rel="noreferrer"
                  aria-label={`${social.label}: ${social.handle}`}
                >
                  <Icon name={social.icon} size={18} />
                  <span>
                    <strong>{social.label}</strong>
                    <small>{social.handle}</small>
                  </span>
                  <Icon name="mdi:open-in-new" size={14} class="contact__channel-ext" />
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </section>

    <aside class="contact__endpoint" aria-labelledby="endpoint-title">
      <div class="contact__endpoint-head">
        <div>
          <p class="contact__endpoint-kicker">secure.handshake</p>
          <h3 id="endpoint-title">Direct endpoint</h3>
        </div>
        <span aria-hidden="true">08</span>
      </div>

      {
        email && (
          <a class="contact__email" href={email.url}>
            <span>Email endpoint</span>
            <strong>{email.handle}</strong>
            <Icon name="mdi:arrow-right" size={16} />
          </a>
        )
      }

      <dl class="contact__protocol">
        {
          protocolRows.map((row) => (
            <div>
              <dt>{row.label}</dt>
              <dd>{row.value}</dd>
            </div>
          ))
        }
      </dl>

      <p class="contact__ready">
        <span aria-hidden="true"></span>
        connection ready · {profile.callsign}.dev
      </p>
    </aside>
  </div>
</Section>

<style>
  .contact {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1.04fr) minmax(340px, 0.86fr);
    align-items: stretch;
    gap: clamp(1rem, 2.4vw, 1.5rem);
  }

  .contact::before {
    content: "";
    position: absolute;
    top: 50%;
    left: 46%;
    right: 38%;
    height: 1px;
    pointer-events: none;
    background: linear-gradient(
      to right,
      transparent,
      rgba(203, 166, 247, 0.45),
      rgba(147, 153, 178, 0.18),
      transparent
    );
  }

  .contact__intro,
  .contact__endpoint {
    position: relative;
    min-width: 0;
  }

  .contact__intro {
    display: grid;
    align-content: center;
    gap: clamp(0.8rem, 1.9vh, 1.05rem);
    padding: clamp(1rem, 2vw, 1.35rem);
    border: 1px solid rgba(147, 153, 178, 0.2);
    background:
      linear-gradient(135deg, rgba(203, 166, 247, 0.05), transparent 42%),
      rgba(24, 24, 37, 0.54);
  }

  .contact__state,
  .contact__copy,
  .contact__channels-title,
  .contact__channel small,
  .contact__endpoint-kicker,
  .contact__endpoint-head > span,
  .contact__protocol,
  .contact__email span,
  .contact__ready {
    font-family: var(--font-mono);
  }

  .contact__state {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    width: fit-content;
    color: var(--mauve);
    font-size: 0.68rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .contact__state span,
  .contact__ready span {
    width: 7px;
    height: 7px;
    background: var(--mauve);
    box-shadow: 0 0 0 4px rgba(203, 166, 247, 0.08);
  }

  .contact__title {
    margin: 0;
    max-width: 10ch;
    font-size: clamp(2.15rem, 6vw, 4rem);
    line-height: 0.98;
    letter-spacing: 0;
    color: var(--text);
  }

  .contact__copy {
    max-width: 58ch;
    margin: 0;
    color: var(--subtext-1);
    font-size: clamp(0.95rem, 1.35vw, 1.05rem);
    line-height: 1.58;
  }

  .contact__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
  }

  .contact__channels {
    display: grid;
    gap: 0.55rem;
    margin-top: clamp(0.2rem, 1vh, 0.5rem);
  }

  .contact__channels-title {
    margin: 0;
    color: var(--subtext-0);
    font-size: 0.66rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .contact__channels ul {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.6rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .contact__channel {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 0.65rem;
    min-height: 4.25rem;
    padding: 0.72rem 0.8rem;
    border: 1px solid rgba(147, 153, 178, 0.2);
    background: rgba(30, 30, 46, 0.52);
    transition:
      transform 0.14s ease,
      border-color 0.14s ease,
      background 0.14s ease;
  }

  .contact__channel:hover,
  .contact__channel:focus-visible {
    transform: translateY(-2px);
    border-color: rgba(203, 166, 247, 0.36);
    background: rgba(49, 50, 68, 0.42);
  }

  .contact__channel :global(svg:first-child) {
    color: var(--mauve);
  }

  .contact__channel span {
    display: grid;
    gap: 0.16rem;
    min-width: 0;
  }

  .contact__channel strong {
    color: var(--text);
    font-family: var(--font-heading);
    font-size: 0.96rem;
    font-weight: 650;
  }

  .contact__channel small {
    overflow: hidden;
    color: var(--subtext-0);
    font-size: 0.72rem;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .contact__channel-ext {
    color: var(--overlay-0);
  }

  .contact__endpoint {
    display: grid;
    grid-template-rows: auto auto 1fr auto;
    gap: 0.85rem;
    padding: clamp(1rem, 2vw, 1.3rem);
    border: 1px solid var(--border-dim);
    border-top-color: rgba(203, 166, 247, 0.38);
    background:
      linear-gradient(225deg, rgba(203, 166, 247, 0.045), transparent 38%),
      rgba(24, 24, 37, 0.76);
    box-shadow: var(--pixel-shadow);
    overflow: hidden;
  }

  .contact__endpoint::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background-image:
      linear-gradient(rgba(147, 153, 178, 0.045) 1px, transparent 1px),
      linear-gradient(90deg, rgba(147, 153, 178, 0.035) 1px, transparent 1px);
    background-size: 34px 34px;
    mask-image: linear-gradient(to bottom, black, transparent 82%);
  }

  .contact__endpoint-head,
  .contact__email,
  .contact__protocol,
  .contact__ready {
    position: relative;
    z-index: 1;
  }

  .contact__endpoint-head {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    padding-bottom: 0.8rem;
    border-bottom: 1px solid rgba(147, 153, 178, 0.2);
  }

  .contact__endpoint-kicker {
    margin: 0 0 0.28rem;
    color: var(--mauve);
    font-size: 0.66rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .contact__endpoint h3 {
    margin: 0;
    color: var(--text);
    font-size: clamp(1.1rem, 2.2vw, 1.35rem);
    line-height: 1.1;
  }

  .contact__endpoint-head > span {
    display: grid;
    place-items: center;
    width: 34px;
    height: 34px;
    flex: none;
    border: 1px solid rgba(147, 153, 178, 0.24);
    color: var(--mauve);
    font-size: 0.68rem;
  }

  .contact__email {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 0.28rem 0.7rem;
    align-items: center;
    padding: 0.88rem;
    border: 1px solid rgba(203, 166, 247, 0.3);
    background: rgba(17, 17, 27, 0.42);
    transition:
      border-color 0.14s ease,
      background 0.14s ease,
      transform 0.14s ease;
  }

  .contact__email:hover,
  .contact__email:focus-visible {
    transform: translateY(-2px);
    border-color: rgba(203, 166, 247, 0.55);
    background: rgba(49, 50, 68, 0.36);
  }

  .contact__email span {
    grid-column: 1 / -1;
    color: var(--subtext-0);
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .contact__email strong {
    min-width: 0;
    color: var(--text);
    font-family: var(--font-mono);
    font-size: clamp(0.9rem, 1.8vw, 1.08rem);
    overflow-wrap: anywhere;
  }

  .contact__email :global(svg) {
    color: var(--mauve);
  }

  .contact__protocol {
    display: grid;
    align-content: start;
    margin: 0;
    border: 1px solid rgba(147, 153, 178, 0.18);
    background: rgba(17, 17, 27, 0.28);
  }

  .contact__protocol div {
    display: grid;
    grid-template-columns: minmax(6.5rem, 0.35fr) minmax(0, 1fr);
    gap: 0.85rem;
    padding: 0.68rem 0.76rem;
    border-bottom: 1px solid rgba(147, 153, 178, 0.14);
  }

  .contact__protocol div:last-child {
    border-bottom: 0;
  }

  .contact__protocol dt {
    color: var(--overlay-1);
    font-size: 0.61rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .contact__protocol dd {
    margin: 0;
    color: var(--text);
    font-size: 0.78rem;
    line-height: 1.35;
  }

  .contact__ready {
    display: flex;
    align-items: center;
    gap: 0.45rem;
    margin: 0;
    color: var(--subtext-0);
    font-size: 0.66rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  @media (max-width: 980px) {
    .contact {
      grid-template-columns: 1fr;
    }

    .contact::before {
      display: none;
    }

    .contact__intro {
      align-content: start;
    }
  }

  @media (max-width: 620px) {
    .contact__channels ul {
      grid-template-columns: 1fr;
    }

    .contact__protocol div {
      grid-template-columns: 1fr;
      gap: 0.2rem;
    }
  }

  @media (max-height: 780px) and (min-width: 981px) {
    .contact__title {
      font-size: clamp(1.95rem, 5vw, 3.35rem);
    }

    .contact__intro,
    .contact__endpoint {
      padding: 0.92rem;
    }

    .contact__copy {
      font-size: 0.92rem;
      line-height: 1.48;
    }

    .contact__channel {
      min-height: 3.72rem;
      padding: 0.58rem 0.68rem;
    }

    .contact__protocol div {
      padding-block: 0.54rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .contact__channel,
    .contact__email {
      transition: none;
    }

    .contact__channel:hover,
    .contact__channel:focus-visible,
    .contact__email:hover,
    .contact__email:focus-visible {
      transform: none;
    }
  }
</style>
