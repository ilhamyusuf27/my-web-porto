---
import { Icon } from 'astro-icon/components';

const contactMethods = [
  {
    name: "Email",
    value: "hello@ilhamya.com",
    icon: "mdi:email",
    link: "mailto:hello@ilhamya.com",
    color: "bg-red-50 text-red-600"
  },
  {
    name: "LinkedIn",
    value: "ilhamya",
    icon: "mdi:linkedin",
    link: "https://linkedin.com/in/ilhamya",
    color: "bg-blue-50 text-blue-600"
  },
  {
    name: "GitHub",
    value: "ilhamya",
    icon: "mdi:github",
    link: "https://github.com/ilhamya",
    color: "bg-gray-50 text-gray-600"
  },
  {
    name: "Discord",
    value: "ilhamya",
    icon: "mdi:discord",
    link: "https://discord.com/users/ilhamya",
    color: "bg-indigo-50 text-indigo-600"
  }
];
---

<section class="py-20 bg-white" id="contact">
  <div class="max-w-5xl mx-auto px-6">
    <!-- Header -->
    <div class="text-center mb-16 fade-up">
      <h2 class="text-4xl font-bold mb-4">
        <span class="text-5xl font-bold text-gray-900 mb-12">
          Get in Touch
        </span>
      </h2>
      <p class="text-gray-600 max-w-2xl mx-auto">
        Feel free to reach out! I'm always open to discussing new projects, creative ideas, or opportunities.
      </p>
    </div>

    <!-- Contact Methods Grid -->
    <div class="grid md:grid-cols-2 gap-6 max-w-3xl mx-auto">
      {contactMethods.map((method, index) => (
        <a
          href={method.link}
          target="_blank"
          rel="noopener noreferrer"
          class="fade-up p-6 rounded-2xl border border-gray-100 hover:border-purple-500 transition-all duration-300 group"
          style={`animation-delay: ${index * 100}ms`}
        >
          <div class="flex items-center gap-4">
            <div class={`w-12 h-12 flex items-center justify-center rounded-xl ${method.color} group-hover:scale-110 transition-transform duration-300`}>
              <Icon name={method.icon} class="w-6 h-6" />
            </div>
            <div>
              <h3 class="text-sm font-medium text-gray-500">{method.name}</h3>
              <p class="text-base font-medium text-gray-900">{method.value}</p>
            </div>
          </div>
        </a>
      ))}
    </div>

    <!-- Additional Info -->
    <div class="mt-16 text-center fade-up">
      <div class="inline-block p-6 rounded-2xl bg-purple-50">
        <h3 class="text-lg font-semibold mb-2 text-purple-900">Available for Work</h3>
        <p class="text-purple-700">Monday - Friday • 9:00 AM - 6:00 PM (GMT+7)</p>
      </div>
    </div>
  </div>
</section>

<style>
  .fade-up {
    opacity: 0;
    transform: translateY(20px);
  }

  .animate {
    opacity: 1;
    transform: translateY(0);
    transition: all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1);
  }
</style>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("animate");
        }
      });
    },
    {
      threshold: 0.1,
    }
  );

  document.querySelectorAll(".fade-up").forEach((el) => observer.observe(el));
</script>